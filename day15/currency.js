var alertMessage = document.getElementById("alert");
var 
function exchange(){
    var fx = parseInt(document.getElementById("forex").value);
    if( fx<0 || fx == null || !Number.isInteger(fx)){
        alertMessage = "Invalid!";
        return;
    }
    else if(fx >=0){
        if(from==="VND"){
            
        }
    }


}